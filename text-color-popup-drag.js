(()=>{const attach=(bar)=>{if(!bar||bar.dataset.apanamDragReady==='1')return;bar.dataset.apanamDragReady='1';bar.style.touchAction='none';const blocked=e=>!!e.target.closest?.('button,input,select,textarea,a');let drag=null;bar.addEventListener('pointerdown',e=>{if(e.button!==undefined&&e.button!==0)return;if(blocked(e))return;const r=bar.getBoundingClientRect();drag={id:e.pointerId,x:e.clientX,y:e.clientY,left:r.left,top:r.top};bar.style.position='fixed';bar.style.left=r.left+'px';bar.style.top=r.top+'px';bar.style.right='auto';bar.style.bottom='auto';bar.style.transform='none';bar.setPointerCapture?.(e.pointerId);e.preventDefault()},true);bar.addEventListener('pointermove',e=>{if(!drag||e.pointerId!==drag.id)return;const maxX=Math.max(0,innerWidth-bar.offsetWidth),maxY=Math.max(0,innerHeight-bar.offsetHeight);bar.style.left=Math.max(0,Math.min(maxX,drag.left+e.clientX-drag.x))+'px';bar.style.top=Math.max(0,Math.min(maxY,drag.top+e.clientY-drag.y))+'px';e.preventDefault()},true);const stop=e=>{if(drag&&(e.pointerId===undefined||e.pointerId===drag.id))drag=null};bar.addEventListener('pointerup',stop,true);bar.addEventListener('pointercancel',stop,true)};const scan=()=>{const p=document.getElementById('apanamPalette');if(p)attach(p)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',scan);else scan();new MutationObserver(scan).observe(document.body,{childList:true,subtree:true});})();