(()=>{if(window.__apanamUploadDragFix)return;window.__apanamUploadDragFix=1;function bind(el){if(!el||el.dataset.dragFix==='1')return;el.dataset.dragFix='1';el.dataset.locked='0';el.style.pointerEvents='auto';el.style.touchAction='none';el.style.cursor='move';const img=el.querySelector('img');if(img){img.style.pointerEvents='none';img.draggable=false}el.addEventListener('pointerdown',e=>{if(e.target.closest('.handle,.rotateHandle'))return;e.preventDefault();e.stopPropagation();if(typeof select==='function')select(el);const sx=e.clientX,sy=e.clientY,sl=el.offsetLeft,st=el.offsetTop,id=e.pointerId;try{el.setPointerCapture(id)}catch(_){}const move=ev=>{if(ev.pointerId!==id)return;el.style.left=sl+(ev.clientX-sx)+'px';el.style.top=st+(ev.clientY-sy)+'px'};const up=ev=>{if(ev.pointerId!==id)return;el.removeEventListener('pointermove',move);el.removeEventListener('pointerup',up);el.removeEventListener('pointercancel',up);try{el.releasePointerCapture(id)}catch(_){}};el.addEventListener('pointermove',move);el.addEventListener('pointerup',up);el.addEventListener('pointercancel',up)},true)}function scan(){document.querySelectorAll('#stage .element').forEach(el=>{if(el.querySelector('img'))bind(el)})}function loadAutoEditable(){if(window.__apanamAutoPosterTextLayers||document.querySelector('script[data-auto-poster-layers]'))return;const s=document.createElement('script');s.src='auto-poster-text-layers.js?v=8278c824';s.dataset.autoPosterLayers='1';document.body.appendChild(s)}function init(){const stage=document.getElementById('stage'),input=document.getElementById('imageUpload');if(!stage)return;scan();loadAutoEditable();new MutationObserver(scan).observe(stage,{childList:true,subtree:true});if(input)input.addEventListener('change',()=>setTimeout(scan,150))}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init()})();