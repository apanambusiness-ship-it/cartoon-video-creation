"use client";
import { useState } from "react";
const choices=[{id:"registration",label:"₹10 registration"},{id:"basic",label:"₹199 Basic"},{id:"creator",label:"₹499 Creator"},{id:"studio",label:"₹999 Studio"}];
export default function TestCheckout(){const [message,setMessage]=useState("");const [loading,setLoading]=useState<string|null>(null);async function start(productId:string){setLoading(productId);setMessage("");try{const response=await fetch("/api/checkout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({productId})});const data=await response.json();if(!response.ok)throw new Error(data.error);window.location.assign(data.url)}catch(error){setMessage(error instanceof Error?error.message:"Test checkout unavailable.");setLoading(null)}}return <section className="test-checkout"><p className="eyebrow">STRIPE TEST MODE</p><h2>Payment flow test karein.</h2><p>Yeh Stripe sandbox hai. Test card se checkout test hoga; koi real paisa, payout ya customer charge nahi hoga.</p><div>{choices.map(choice=><button type="button" key={choice.id} disabled={loading!==null} onClick={()=>start(choice.id)}>{loading===choice.id?"Opening…":`Test ${choice.label}`}</button>)}</div>{message&&<small>{message}</small>}</section>}
