"use client";
import { useState } from "react";
export default function Creator(){const [idea,setIdea]=useState("");const [format,setFormat]=useState("Cartoon video");const [message,setMessage]=useState("Your concept will appear here.");return <div className="creator"><div><label>YOUR STORY IDEA</label><textarea value={idea} onChange={e=>setIdea(e.target.value)} placeholder="Ek chhote shehar ki ladki apne cartoon hero ke saath duniya bachati hai…"/><div className="fields"><select value={format} onChange={e=>setFormat(e.target.value)}><option>Cartoon video</option><option>Movie poster</option><option>Political poster</option><option>Serial episode</option></select><select><option>Hindi</option><option>English</option><option>Hinglish</option></select></div><button className="button" onClick={()=>setMessage(idea.trim()?`${format} concept ready: ${idea.trim()}`:"Pehle apna story idea likhiye.")}>Generate concept →</button><small>AI rendering launch configuration के बाद उपलब्ध होगी।</small></div><aside><p className="eyebrow">YOUR CREATION</p><div className="spark">✦</div><h2>{message}</h2><p>₹299/month Creator plan में monthly credits और AI creation tools मिलेंगे। Checkout अभी launch से पहले disabled है।</p></aside></div>}
