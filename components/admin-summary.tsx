type Stats={creators:number;total:number;requested:number;inProgress:number;ready:number;delivered:number};
export default function AdminSummary({stats}:{stats:Stats}){const cards=[['Creators',stats.creators,'Signed-in users'],['New',stats.requested,'Needs your review'],['In progress',stats.inProgress,'Work underway'],['Ready',stats.ready,'Send delivery link'],['Delivered',stats.delivered,'Completed designs']];return <section className="admin-summary"><p className="eyebrow">OPERATIONS OVERVIEW</p><h2>Today’s workspace</h2><div>{cards.map(([name,value,note])=><article key={String(name)}><span>{name}</span><b>{value}</b><small>{note}</small></article>)}</div><p className="admin-total">Total manual requests: <b>{stats.total}</b></p></section>}
