"use client";
import { useState } from "react";
type Request={id:string;status:string;language:string;prompt:string;createdAt:string};
const labels:Record<string,string>={requested:"New request",in_progress:"In progress",ready:"Ready",delivered:"Delivered"};
export function MyProjects({requests}:{requests:Request[]}){return <section className="my-projects"><p className="eyebrow">MY PROJECTS</p><h2>Apne poster requests track kijiye.</h2>{requests.length?<div>{requests.map(item=><article key={item.id}><span className={`status ${item.status}`}>{labels[item.status]||item.status}</span><b>Manual poster · {item.language}</b><p>{item.prompt.slice(0,150)}</p><small>{new Date(item.createdAt).toLocaleDateString("en-IN")}</small></article>)}</div>:<p className="empty">Abhi koi manual poster request nahi hai.</p>}</section>}
export function AdminRequestStatus({initial}:{initial:Request[]}){const [items,setItems]=useState(initial);const [saving,setSaving]=useState<string|null>(null);async function update(id:string,status:string){setSaving(id);try{const response=await fetch("/api/admin/requests",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,status})});if(!response.ok)throw new Error();setItems(rows=>rows.map(row=>row.id===id?{...row,status}:row))}finally{setSaving(null)}}return <section className="request-list"><p className="eyebrow">MANUAL DESIGN REQUESTS</p><h2>Manage requests</h2>{items.length?items.map(item=><article key={item.id}><div><span className={`status ${item.status}`}>{labels[item.status]||item.status}</span><b>{item.language}</b></div><pre>{item.prompt}</pre><select value={item.status} disabled={saving===item.id} onChange={event=>update(item.id,event.target.value)}>{Object.entries(labels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></article>):<p>No manual design requests yet.</p>}</section>}
